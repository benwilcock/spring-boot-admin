spring:
  application:
    name: spring-boot-admin
  boot:
    admin:
      ui:
        public-url: http://spring-boot-admin.default.apps.example.com

management:
  endpoints:
    web:
      exposure:
        include: "*"