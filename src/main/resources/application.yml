spring:
  application:
    name: spring-boot-admin
  boot:
    admin:
      ui:
        public-url: http://spring-boot-admin.${NAMESPACE}.${DOMAIN}

management:
  endpoints:
    web:
      exposure:
        include: "*"