spring:
  application:
    name: spring-boot-admin
  boot:
    admin:
      ui:
        public-url: http://${spring.application.name}.${NAMESPACE}.${DOMAIN}

management:
  endpoints:
    web:
      exposure:
        include: "*"